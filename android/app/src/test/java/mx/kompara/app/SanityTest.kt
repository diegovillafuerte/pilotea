package mx.kompara.app

import org.junit.Assert.assertEquals
import org.junit.Test

// Placeholder proving the unit-test toolchain runs; real tests arrive with B-025+.
class SanityTest {
    @Test
    fun `test toolchain executes`() {
        assertEquals(4, 2 + 2)
    }
}
