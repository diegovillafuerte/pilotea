<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-only manifest overlay: allow cleartext HTTP to the dev backend on the
  device's loopback (adb reverse) and the emulator host alias. Release builds
  keep the platform default (cleartext blocked everywhere) — see
  res/xml/network_security_config_debug.xml.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application android:networkSecurityConfig="@xml/network_security_config_debug" />
</manifest>
