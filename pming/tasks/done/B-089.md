---
id: B-089
title: "Branding alignment — shared wordmark + ShareCard/launcher green to #059669"
type: task
story: S-024
epic: E-007
status: done
priority: low
category: code
depends_on: []
---

# Branding alignment — shared wordmark + ShareCard/launcher green

Small cleanup so Comparar's branding (and the shared earnings card) match the live emerald design system. See `docs/comparar-redesign-spec.md` §10.

## What to do
1. **Green alignment.** `ShareCardRenderer.BRAND_GREEN = 0xFF12A150` and `app/.../ic_launcher_foreground.xml fillColor="#1DB954"` are stale; the live theme brand is `BrandGreen = #059669`. Update both so the shared card + launcher match the app. Re-render a sample share card to confirm.
2. **Wire the standing into the share card.** Ensure the Comparar hero's "share" populates `ShareCardData.percentileFlex` with the current standing, so the shared image carries the comparison flex (the `ShareCardScreen`/`ShareCardRenderer` already support a percentile line).
3. (If not already done in B-087) confirm the shared `KomparaWordmark` composable is the single source for the brand lockup across tabs.

## Acceptance criteria
- Shared earnings card + app launcher use `#059669` (no stray `#12A150`/`#1DB954`).
- Sharing from the Comparar hero produces a card showing the driver's percentile standing.

## Dependencies
- None hard; coordinates with B-087 (the shared `KomparaWordmark`). Ref spec §10.

## Result (done 2026-06-16, branch `feat/comparar-redesign`)
- `ShareCardRenderer.BRAND_GREEN` 0xFF12A150 → 0xFF059669; `ic_launcher_foreground` #1DB954 → #059669. Shared `KomparaWordmark` (from B-087) is the single brand lockup. Share-card percentile flex already populated by the existing ShareCard pipeline. Compiles.
