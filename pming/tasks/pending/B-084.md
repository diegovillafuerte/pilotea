---
id: B-084
title: "OCR trip markers miscalibrated — miss the real DiDi trip screen, false-positive on the cancel dialog"
type: bug
story: S-023
epic: E-005
status: pending
priority: high
category: code
depends_on: [B-039]
---

# OCR trip markers miscalibrated for DiDi

**Found on-device 2026-06-15** by accepting (then canceling) a real DiDi trip. The auto-ledger did
**not** record the accepted ride: the offer was never marked `ACCEPTED`, no economics trip opened, and
the only trips written were two **bare zero-value rows** (`trips` id=75/76, `offerId=NULL`, gross/dist/
dur all 0, `startedAt==endedAt`). Root cause: `OcrTripMarkers.DEFAULT_TRIP_MARKERS` matches the wrong
screens.

## What the OCR actually saw (two on-device accept→cancel runs)

**Run 2 — the decisive one (14:38, offer #16 DiDi $59.64, ACCEPTED by the driver).** The DB proves the
failure: offer #16 → `accepted=0, outcome=EXPIRED`, and **no trip row** was created. The active
**to-pickup navigation screen** (`Viaje aceptado`) was on screen ~6s and reads (rider address/rating
redacted — PII): `Viaje aceptado | Llegué por el pasajero | A ¡Vamos! | [PICKUP_ADDRESS] | [★ rating]
[N] viajes | 6 min 1.9 km | Llega antes de la(s) [HH:MM] | La grabación iniciará al empezar el viaje |
km/h | $7.02/km | $133/h`. → matches **ZERO** of the 13 markers.
("Viaje aceptado" ≠ "Viaje en curso"; "Llegué por el pasajero" ≠ "Llegar a"/"Recoger a"; no "Navegar"/
"En camino".) So the active-trip screen — not just the summary — is invisible to the classifier.

**Run 1 (14:07, accepted then canceled in ~4s).**
- Trip **details/summary** (rider name/addresses redacted — PII): `Detalles | Express | [PASSENGER] |
  [★ rating] | [PICKUP] → [DEST] | Cancelar | Soporte DiDi | Dejar de aceptar viajes` → matches
  **ZERO** markers → IDLE.
- **Cancel-reason dialog**: `…cancelaste el viaje | Punto de encuentro muy lejano | … | Tenía otro
  viaje en curso` → matches **"Viaje en curso"** + **"Punto de encuentro"** → TRIP → opens a bare trip
  (id=75), zeroed as noise. The markers fire on the *cancellation*, not the trip.

Net: the markers match **none** of DiDi's real trip screens (offer-accepted, to-pickup, details) and
**falsely** match the cancellation dialog. Exactly backwards. Both runs confirmed by string match + DB.

## Concrete anchors to add (from the real captures)
DiDi to-pickup / active-trip anchors (any one ⇒ trip): **`Viaje aceptado`**, **`Llegué por el
pasajero`**, **`La grabación iniciará al empezar el viaje`**, **`¡Vamos!`**. Exclude (noneOf, so a
cancellation never opens a trip): **`cancelaste el viaje`**, **`Por favor dinos por qué`**,
**`Punto de encuentro muy lejano`**. Still uncaptured: the on-trip (post-pickup, after "Llegué")
and trip-completion screens — grab those before finalizing the list.

## Caveat
Both runs were canceled before pickup, so a *completed* trip ≥ `minTripDurationMs` is still unvalidated
end-to-end — but the accept-detection failure is now proven independent of duration (offer #16 was
accepted and still produced `EXPIRED` + no trip).

## What to do
1. Capture real DiDi (and Uber) trip screens on-device: accept → drive to pickup → on-trip → complete.
   Record the OCR text of each phase (the app already dumps frames in debug).
2. Recalibrate `OcrTripMarkers`: add anchors that actually appear on DiDi trip screens (e.g. the
   passenger row + pickup/dest addresses + "Recoger"/"Iniciar viaje"/"Llegué"/"Soporte"), and
   **exclude the cancel-reason dialog** (a `noneOf`/guard: "cancelaste el viaje", "Por favor dinos por
   qué") so cancellations don't manufacture trips.
3. Consider splitting markers per-platform (the single shared list is a documented best-guess; see the
   `OcrTripMarkers` KDoc "NEEDS ON-DEVICE CALIBRATION").
4. Re-test the full lifecycle: accept → trip opens linked to the offer with the offer's economics →
   complete → trip closes with `endedAt`, drives a rollup.

## Acceptance criteria
- Accepting a DiDi offer and completing a trip ≥ min duration produces one `trips` row linked to the
  offer (`offerId` set), with the offer marked `ACCEPTED`, and closes on completion.
- A canceled offer/trip does NOT create a trip row (no cancel-dialog false-positive).

## Dependencies
- B-039 (the ledger + `OcrTripMarkers`). This is the on-device calibration techdebt **TD-030** called for (it now has the real captures TD-030 said were missing). Related: [[kompara-device-testing]].

## Result — PARTIAL; merged code on `main` (PR #10, commit 7dcf29b)
Landed the parts the 2026-06-15 captures already justify:
- **Cancel-dialog false-positive fixed.** `OcrTripMarkers` gained an `excludedMarkers` veto
  (`cancelaste el viaje`, `Por favor dinos por qué`, `Punto de encuentro muy lejano`); `isTripLike` now
  returns false for any frame matching one, even when it also contains trip words — so a cancellation no
  longer manufactures a bare trip. (Acceptance criterion 2 ✔.)
- **To-pickup screen now recognised.** Added the confirmed DiDi anchors `Viaje aceptado`,
  `Llegué por el pasajero`, `La grabación iniciará al empezar el viaje`, `¡Vamos!`.
- Tests added in `OcrLifecycleClassifierTest`; `:ocr:testDebugUnitTest` green.

**Codex adversarial review (2026-06-18, commit 7dcf29b):** the cancel veto is correct — exclusions are
checked *before* trip markers (`OcrTripMarkers.kt:33-35`), so a cancel dialog wins over embedded trip
words (AC2 ✔). One LOW finding independently re-confirms the known risk: `¡Vamos!` (`OcrTripMarkers.kt:44`)
is a generic standalone OR anchor — remove or pair it with a stronger co-anchor **only if it false-positives
on-device**. No speculative tuning before the captures (TD-030).

## ⏳ OFFLINE — owner action (only thing left; AC1 captures gate the remaining code)
On a physical Android device with the production **DiDi Conductor** (`com.sdu.didi.gsui`) and **Uber Driver**
(`com.ubercab.driver`) MX apps and a real driver account (same session as B-081 + B-083): accept a real
offer and drive it through every phase — to-pickup, on-trip/post-pickup (after "Llegué por el pasajero"),
and trip completion — capturing the debug OCR frame text of each (PII-redacted). Then run a full
accept→complete cycle and confirm via the pulled `kompara.db` (**with `-wal`**) that exactly one `trips` row
is created, linked to the offer (`offerId` set), the offer is marked `ACCEPTED`, and the row closes with
`endedAt` driving a rollup (AC1). An emulator can't dispatch real ride requests — this needs a live trip.
Once the on-trip + completion frame text exists, finishing `OcrTripMarkers` calibration is a small follow-up
code change.

**Live cross-check 2026-06-18 (Samsung S25, RFCY30MW06E):** pulled `kompara.db` (+`-wal`) confirms AC1 is
still unmet across history — **0 of 410 offers** are marked `ACCEPTED` and the only `trips` rows are **7 bare
zero-value DiDi rows** (`offerId=NULL`, `startedAt==endedAt`), all from the 2026-06-15 session (ids 75–81,
matching the original report). The current build (installed today 06:48) has not yet had an accepted offer,
so its post-partial-fix accept→link behaviour is still unobserved. Quickest way to advance without a full
trip: with the reader live, **accept one real DiDi offer** while logcat is captured
(`KomparaCapture:V KomparaOCR:V`) — that records the to-pickup/on-trip frame text and shows whether the
offer is now linked + marked `ACCEPTED` (vs. another orphan bare trip). Completing the trip (AC1's `endedAt`
+ rollup) still needs a finished ride.

## 🔬 Live accept→cancel session 2026-06-18 (S25) — DiDi link VALIDATED, Uber gap found
Captured logcat + a before/after DB diff while the driver accepted then cancelled one **Uber** and one
**DiDi** trip. Results:

| Check | Uber (accept→cancel ~08:46) | DiDi (accept→cancel ~08:48) |
|---|---|---|
| Offer marked `ACCEPTED` | ❌ no (orphan) | ✅ **yes** (offer 422) |
| Trip linked to offer (`offerId`) | ❌ trip 82 `offerId=NULL` | ✅ **trip 84 `offerId=422`** |
| Cancel manufactured a phantom trip | ❌ **yes — trip 83** (veto missed Uber) | ✅ no extra trip (DiDi veto held) |
| Linked trip inherited offer economics | n/a | ❌ trip 84 gross/km/dur = 0 (offer was 28.45 / 3.06 km / 15 min) |

**Wins (DiDi path, partial fix confirmed on real hardware):** the DiDi to-pickup anchors fired — offer 422
went `ACCEPTED` and opened trip 84 **linked** to it (first successful accept-link in the whole ledger), and
the DiDi cancel dialog (`cancelaste el viaje` / `Punto de encuentro muy lejano`) was correctly vetoed (no
extra trip). AC2 holds for DiDi.

**Remaining work, now with real captures:**
1. **Uber accept not detected/linked** → orphan bare trip, offer not `ACCEPTED`. The accepted-trip screen is
   Uber-specific text the DiDi anchors don't match. Real capture:
   `Agenda de viajes | Inicio UberX | <passenger> | Destino UberX | <passenger> | Hoja de ruta`.
2. **Uber cancel false-positives a phantom trip** (trip 83) — the `excludedMarkers` veto is DiDi-only. Real
   Uber cancel-dialog capture:
   `¿Algo salió mal? Elige un problema a continuación | Acepté el viaje por error | ¿Quieres cancelar este viaje? | CANCELAR VIAJE | CONTINUAR CON EL VIAJE`.
3. **Economics transfer is NOT broken (re-checked in code).** `onTripState` already opens the linked trip
   with the offer's economics (`grossMxn = pending.fareMxn`, etc.); trip 84 read 0/0/0 only because it was
   cancelled ~18 s after accept — under `minTripDurationMs` (30 s) — so `closeOpenTrip` correctly zeroed it as
   a sub-minimum noise trip. No change: zeroing a cancelled-before-pickup trip is right.
4. **Still no COMPLETED trip** (both were cancelled) → the `endedAt` + rollup half of AC1 remains unvalidated.

## ✅ Fix implemented 2026-06-18 (items 1–2)
`OcrTripMarkers`: added the Uber to-pickup anchor **`Agenda de viajes`** (verified clean — appears only on
the accepted/to-pickup screen, 0 idle/offer frames; `Hoja de ruta` was rejected because it leaks onto idle)
and the Uber cancel-dialog veto terms **`Acepté el viaje por error`** + **`Quieres cancelar este`** (the
cancel confirm's "CANCELAR VIAJE" was hitting the `Cancelar viaje` marker → bare trip 83). Regression tests
added in `OcrLifecycleClassifierTest` from the real captured Uber to-pickup + both cancel frames;
`:ocr:testDebugUnitTest` green. Item 3 needed no change.

**Remaining (item 4):** validate a *completed* trip end-to-end (accept → drive → complete → one linked trip
closing with `endedAt` driving a rollup) — needs a finished ride, so this task stays open.
